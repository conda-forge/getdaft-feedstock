diff --git a/src/daft-dashboard/build.rs b/src/daft-dashboard/build.rs
index ea8a6ab..0095034 100644
--- a/src/daft-dashboard/build.rs
+++ b/src/daft-dashboard/build.rs
@@ -29,7 +29,7 @@ fn default_main(out_dir: &str) -> Result<(), Box<dyn std::error::Error>> {
     println!("cargo:rerun-if-changed=build.rs");
 
     // Check if bun is installed
-    let bun_available = Command::new("bun")
+    let bun_available = Command::new("npm")
         .arg("--version")
         .output()
         .map(|_| true)
@@ -48,7 +48,7 @@ fn default_main(out_dir: &str) -> Result<(), Box<dyn std::error::Error>> {
     }
 
     // Install dependencies
-    let install_status = Command::new("bun")
+    let install_status = Command::new("npm")
         .current_dir("./frontend")
         .args(["install"])
         .status()?;
@@ -56,7 +56,7 @@ fn default_main(out_dir: &str) -> Result<(), Box<dyn std::error::Error>> {
     assert!(install_status.success(), "Failed to install dependencies");
 
     // Run `bun run build`
-    let mut cmd = Command::new("bun");
+    let mut cmd = Command::new("npm");
     let status = cmd.current_dir("./frontend");
 
     let status = if cfg!(debug_assertions) {
